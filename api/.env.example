DB_HOST=localhost
DB_PORT=5432
DB_NAME=osa_db
DB_USER=osa_user
DB_PASSWORD=change-me

# Clé expert — stocker le SHA-256 en base via patch_mg_api_key_registry.sql
API_EXPERT_KEY=change-me

# CORS — restreindre en production
CORS_ORIGINS=*
